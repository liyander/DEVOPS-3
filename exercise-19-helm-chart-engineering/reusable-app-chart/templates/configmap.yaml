{{- if .Values.configMaps.enabled }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "reusable-app.fullname" . }}-config
  labels:
    {{- include "reusable-app.labels" . | nindent 4 }}
data:
  {{- range $key, $value := .Values.configMaps.data }}
  {{ $key }}: {{ $value | quote }}
  {{- end }}
{{- end }}
