{{- if .Values.serviceAccount.create }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "reusable-app.serviceAccountName" . }}
  labels:
    {{- include "reusable-app.labels" . | nindent 4 }}
{{- end }}
